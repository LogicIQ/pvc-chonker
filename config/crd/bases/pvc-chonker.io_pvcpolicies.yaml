---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: pvcpolicies.pvc-chonker.io
spec:
  group: pvc-chonker.io
  names:
    kind: PVCPolicy
    listKind: PVCPolicyList
    plural: pvcpolicies
    singular: pvcpolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PVCPolicy is the Schema for the pvcpolicies API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PVCPolicySpec defines the desired state of PVCPolicy
            properties:
              selector:
                description: Selector specifies which PVCs this policy applies to
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              template:
                description: Template defines the expansion configuration
                minProperties: 1
                properties:
                  cooldown:
                    description: Cooldown is the minimum time between expansions
                    type: string
                  enabled:
                    description: Enabled controls whether auto-expansion is enabled
                    type: boolean
                  increase:
                    description: Increase specifies the expansion amount (percentage
                      or absolute)
                    pattern: ^([0-9]+(\.[0-9]*)?%?|[0-9]+(\.[0-9]*)?[KMGTPE]i?)$
                    type: string
                  inodesThreshold:
                    description: InodesThreshold is the inode usage percentage that
                      triggers expansion
                    pattern: ^([0-9]+(\.[0-9]*)?%?)$
                    type: string
                  maxSize:
                    anyOf:
                    - type: integer
                    - type: string
                    description: MaxSize is the maximum size limit for the PVC
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  minScaleUp:
                    anyOf:
                    - type: integer
                    - type: string
                    description: MinScaleUp is the minimum expansion amount
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  threshold:
                    description: Threshold is the storage usage percentage that triggers
                      expansion
                    pattern: ^([0-9]+(\.[0-9]*)?%?)$
                    type: string
                type: object
            required:
            - selector
            - template
            type: object
          status:
            description: PVCPolicyStatus defines the observed state of PVCPolicy
            properties:
              lastUpdated:
                description: LastUpdated is the timestamp when the policy was last
                  processed
                format: date-time
                type: string
              matchedPVCs:
                description: MatchedPVCs is the number of PVCs currently matched by
                  this policy
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
