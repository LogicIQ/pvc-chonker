apiVersion: v1
kind: Service
metadata:
  name: pvc-chonker-webhook-service
  namespace: pvc-chonker-system
spec:
  ports:
  - name: webhook-server
    port: 443
    protocol: TCP
    targetPort: 9443
  selector:
    control-plane: controller-manager