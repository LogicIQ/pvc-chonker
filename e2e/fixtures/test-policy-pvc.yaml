apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-policy-pvc
  labels:
    test-policy: enabled  # Matches PVCPolicy selector
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: expandable-local